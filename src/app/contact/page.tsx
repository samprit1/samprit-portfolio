import { getSiteSettings } from '@/sanity/projects'; import { Instagram, Linkedin, Mail } from 'lucide-react'; export default async function Contact(){const settings=await getSiteSettings();const links=[[settings.email,'Email',Mail],[settings.instagram,'Instagram',Instagram],[settings.linkedin,'LinkedIn',Linkedin]];return <div className="mx-auto max-w-3xl"><p className="text-xs font-bold text-[#e93434]">CONTACT</p><h1 className="mt-2 text-3xl font-bold">{settings.contactTitle}</h1><p className="mt-4 text-[var(--muted)]">{settings.contactBody}</p><div className="mt-8 grid gap-3 sm:grid-cols-2">{links.map(([url,name,I]:any)=><a className="focus flex items-center gap-4 rounded-xl border border-[var(--line)] bg-[var(--surface)] p-5 hover:bg-[var(--surface2)]" href={name==='Email'?`mailto:${url}`:url} key={name}><I size={21}/><span className="font-semibold">{name}</span></a>)}</div></div>}
