import { searchProjects } from '@/lib/utils'; import { getPortfolioProjects } from '@/sanity/projects'; import { VideoCard } from '@/components/PortfolioCards'; export default async function({searchParams}:{searchParams:{q?:string}}){const q=searchParams.q||'';const found=searchProjects(q,await getPortfolioProjects());return <div className="mx-auto max-w-[1280px]"><h1 className="mb-6 text-xl font-bold">Search results for <span className="text-[var(--muted)]">{q||'all projects'}</span></h1>{found.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{found.map(p=><VideoCard key={p.id} project={p}/>)}</div>:<p className="text-[var(--muted)]">No projects found. Try another search.</p>}</div>}
