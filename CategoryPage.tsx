import { CategoryChips } from './CategoryChips'; import { Project, ProjectType, typeLabels } from '@/data/projects'; import { ArtCard, ShortCard, VideoCard } from './PortfolioCards'; import { FrameGallery } from './FrameGallery'; import { MediaMarquee } from './MediaMarquee'
export function CategoryPage({type,projects}:{type:ProjectType;projects:Project[]}){const shorts=type==='short-form', art=type==='frame'||type==='poster';return <div className="mx-auto max-w-[1280px]"><CategoryChips/><div className="mb-5 mt-5"><p className="text-xs text-[var(--muted)]">PORTFOLIO</p><h1 className="mt-1 text-2xl font-bold">{typeLabels[type]}</h1></div>{type==='frame'?<FrameGallery projects={projects}/>:type==='motion-design'?<MediaMarquee projects={projects} kind="motion-design"/>:type==='poster'?<MediaMarquee projects={projects} kind="poster"/>:shorts?<div className="grid grid-cols-2 gap-4 sm:grid-cols-4 lg:grid-cols-6">{projects.map(p=><ShortCard key={p.id} project={p}/>)}</div>:<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{projects.map(p=>art?<ArtCard key={p.id} project={p}/>:<VideoCard key={p.id} project={p}/>)}</div>}</div>}
