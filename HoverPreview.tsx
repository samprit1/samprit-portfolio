'use client'
import { useState } from 'react'; import { Project } from '@/data/projects'; import { getEmbedUrl } from '@/lib/utils'; import { ProjectArt } from './ProjectArt'

/** A single preview is only loaded while its card is hovered — the homepage stays light. */
export function HoverPreview({project}:{project:Project;vertical?:boolean}){const [hovered,setHovered]=useState(false);const playable=project.videoUrl&&!project.videoUrl.includes('YOUR_');const params='autoplay=1&mute=0&controls=0&playsinline=1&rel=0';return <div onMouseEnter={()=>setHovered(true)} onMouseLeave={()=>setHovered(false)} onFocus={()=>setHovered(true)} onBlur={()=>setHovered(false)} className="relative h-full w-full">{hovered&&playable?(project.videoType==='mp4'?<video className="pointer-events-none h-full w-full object-cover" src={project.videoUrl} autoPlay loop playsInline/>:<iframe className="pointer-events-none h-full w-full border-0 object-cover" src={`${getEmbedUrl(project)}?${params}`} title={`${project.title} preview`} allow="autoplay; encrypted-media; picture-in-picture"/>):<ProjectArt project={project}/>} {hovered&&playable&&<span className="pointer-events-none absolute bottom-2 left-2 rounded bg-black/70 px-2 py-1 text-[10px] font-semibold text-white">Preview</span>}</div>}
